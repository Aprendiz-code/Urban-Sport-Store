# Deployment

The backend is prepared for deployment in a Node.js environment with PostgreSQL. For local development, use the provided .env file and a PostgreSQL instance. Docker Compose is also included for containerized local runs.
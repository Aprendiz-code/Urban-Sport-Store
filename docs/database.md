# Database

The backend uses PostgreSQL through Prisma ORM. The main entities cover users, roles, addresses, catalog data, carts, orders, payments, shipments, coupons, reviews, audits, and password reset flows.